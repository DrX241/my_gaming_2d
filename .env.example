# Copier vers .env et renseigner la cle (ne jamais commiter .env ni la coller dans le chat)
GEMINI_API_KEY=
# Optionnel : voir les modeles sur la doc Google AI
GEMINI_MODEL=gemini-2.0-flash
# Port du jeu + API (défaut 8788 si non défini)
PORT=8788
